{% extends 'default.twig' %}

{% block content %}
<div class="wrapper">
    <h1>{{ page.title}}</h1>

    <div class="p-desc">
        {% block content_inner %}{% endblock %}
    </div>

    {#{% include 'recent_portfolio.twig' %}#}
</div>
{% endblock %}
